[package]
name = "archimedes-incentive"
version = "0.1.0"
edition = "2021"

[dependencies]
archimedes-core = { path = "../core" }
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }


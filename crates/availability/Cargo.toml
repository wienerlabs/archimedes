[package]
name = "archimedes-availability"
version = "0.1.0"
edition = "2021"

[dependencies]
archimedes-core = { path = "../core" }
sha2 = "0.10"
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }

